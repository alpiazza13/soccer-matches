- Once the initial backend structure is created, navigate to the backend directory and run the below commands to create and enter virtual environment. 
	-`python -m venv venv`
	-`source venv/bin/activate`
	-`pip install -r requirements.txt`
	
	
		
			
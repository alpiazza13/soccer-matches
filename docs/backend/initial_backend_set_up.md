- Once the initial backend structure is created, navigate to the root directory and run the below commands to create and enter virtual environment. 
	-`python -m venv venv`
	-`source venv/bin/activate`
	-`pip install -r requirements.txt` do this in the backend directory
	
	
		
			